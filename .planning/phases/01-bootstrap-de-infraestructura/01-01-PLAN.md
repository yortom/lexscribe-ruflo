---
id: 01-01
phase: 1
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - .gitignore
  - .nvmrc
  - .editorconfig
  - .prettierrc
  - .eslintrc.cjs
  - packages/shared-types/package.json
  - packages/shared-types/tsconfig.json
  - packages/shared-types/src/index.ts
  - packages/shared-validation/package.json
  - packages/shared-validation/tsconfig.json
  - packages/shared-validation/src/index.ts
  - .env.example
  - README.md
requirements_addressed: [INF-01]
autonomous: true
---

# Plan 01-01: Monorepo init — pnpm workspaces + shared packages + tooling base

## Objective
Establecer el esqueleto del monorepo con pnpm workspaces 9.x, TypeScript base, ESLint/Prettier compartidos, y los dos paquetes compartidos (`shared-types`, `shared-validation`) listos para ser consumidos por `apps/frontend` y `apps/backend`. Sin código de dominio.

## Tasks

<task id="01-01-T1">
<title>Inicializar root package.json + pnpm workspaces + tooling base</title>
<read_first>
- docs/ARQUITECTURA.md (sección 2 Stack tecnológico, sección 3 Estructura de repositorio, sección 15 Versiones runtime fijadas)
- CLAUDE.md (File Organization rules)
- .planning/REQUIREMENTS.md (INF-01)
</read_first>
<action>
Crea los siguientes archivos en la raíz del repositorio:

1. `package.json` (root) con contenido EXACTO:
```json
{
  "name": "lexscribe",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"./apps/*\" run dev",
    "build": "pnpm -r --filter \"./apps/*\" run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "type-check": "pnpm -r run type-check",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "prettier": "^3.3.0",
    "eslint": "^8.57.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0"
  }
}
```

2. `pnpm-workspace.yaml`:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

3. `tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "resolveJsonModule": true,
    "isolatedModules": true
  }
}
```

4. `.nvmrc`: contenido literal `22`

5. `.gitignore` con al menos estas líneas:
```
node_modules
dist
.next
.turbo
coverage
.env
.env.local
*.log
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
```

6. `.editorconfig`:
```
root = true
[*]
indent_style = space
indent_size = 2
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true
```

7. `.prettierrc`:
```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 100,
  "tabWidth": 2
}
```

8. `.eslintrc.cjs`:
```js
module.exports = {
  root: true,
  parser: '@typescript-eslint/parser',
  plugins: ['@typescript-eslint'],
  extends: ['eslint:recommended', 'plugin:@typescript-eslint/recommended'],
  env: { node: true, es2022: true },
  ignorePatterns: ['dist', 'node_modules', '.next', 'coverage'],
};
```

9. `.env.example` (raíz) — template SIN secretos reales:
```
# === Backend ===
NODE_ENV=development
PORT=3001
MONGO_URI=mongodb://mongodb:27017/lexscribe
JWT_SECRET=changeme-jwt-secret
JWT_REFRESH_SECRET=changeme-refresh-secret
APP_ENCRYPTION_KEY=changeme-32-byte-hex-key

# === MinIO ===
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=lexscribe

# === Seed ===
SEED_USER_EMAIL=admin@lexscribe.local
SEED_USER_PASSWORD=changeme

# === Frontend ===
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
```

10. `README.md` (raíz) — solo si no existe ya con contenido. Si existe, NO lo sobrescribas. Si no existe, créalo con un H1 `# Lexscribe` y una línea: `Monorepo. See docs/ARQUITECTURA.md.`
</action>
<acceptance_criteria>
- `test -f package.json && grep -q '"packageManager": "pnpm@9' package.json` → exit 0
- `grep -q '"node": ">=22' package.json` → exit 0
- `grep -q 'apps/\*' pnpm-workspace.yaml` → exit 0
- `grep -q 'packages/\*' pnpm-workspace.yaml` → exit 0
- `test -f tsconfig.base.json && grep -q '"strict": true' tsconfig.base.json` → exit 0
- `test -f .nvmrc && grep -q '^22$' .nvmrc` → exit 0
- `test -f .gitignore && grep -q '^node_modules$' .gitignore && grep -q '^.env$' .gitignore` → exit 0
- `test -f .env.example && grep -q '^MONGO_URI=' .env.example && grep -q '^JWT_SECRET=' .env.example && grep -q '^MINIO_BUCKET=lexscribe' .env.example` → exit 0
- `test -f .prettierrc && test -f .eslintrc.cjs && test -f .editorconfig` → exit 0
- `.env` file MUST NOT exist in repo (verify: `! test -f .env` → exit 0)
</acceptance_criteria>
</task>

<task id="01-01-T2">
<title>Crear paquete shared-types</title>
<read_first>
- docs/ARQUITECTURA.md (sección 3 Estructura de repositorio)
- tsconfig.base.json (just created in T1)
</read_first>
<action>
Crea el paquete `packages/shared-types`:

1. `packages/shared-types/package.json`:
```json
{
  "name": "@lexscribe/shared-types",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
```

2. `packages/shared-types/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

3. `packages/shared-types/src/index.ts` — placeholder export (no domain logic yet):
```ts
export type ISODateString = string;

export interface HealthStatus {
  status: 'ok' | 'error';
  timestamp: ISODateString;
}
```
</action>
<acceptance_criteria>
- `test -f packages/shared-types/package.json && grep -q '"@lexscribe/shared-types"' packages/shared-types/package.json` → exit 0
- `test -f packages/shared-types/tsconfig.json && grep -q '"extends": "../../tsconfig.base.json"' packages/shared-types/tsconfig.json` → exit 0
- `test -f packages/shared-types/src/index.ts && grep -q 'export interface HealthStatus' packages/shared-types/src/index.ts` → exit 0
</acceptance_criteria>
</task>

<task id="01-01-T3">
<title>Crear paquete shared-validation con Zod</title>
<read_first>
- docs/ARQUITECTURA.md (sección 2: Validation row — Zod compartido)
- packages/shared-types/package.json (pattern to mirror)
</read_first>
<action>
Crea el paquete `packages/shared-validation`:

1. `packages/shared-validation/package.json`:
```json
{
  "name": "@lexscribe/shared-validation",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
```

2. `packages/shared-validation/tsconfig.json` (idéntico estructuralmente a shared-types):
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

3. `packages/shared-validation/src/index.ts` — placeholder schema:
```ts
import { z } from 'zod';

export const HealthStatusSchema = z.object({
  status: z.enum(['ok', 'error']),
  timestamp: z.string(),
});

export type HealthStatusInput = z.infer<typeof HealthStatusSchema>;
```
</action>
<acceptance_criteria>
- `test -f packages/shared-validation/package.json && grep -q '"@lexscribe/shared-validation"' packages/shared-validation/package.json` → exit 0
- `grep -q '"zod"' packages/shared-validation/package.json` → exit 0
- `test -f packages/shared-validation/src/index.ts && grep -q 'HealthStatusSchema' packages/shared-validation/src/index.ts` → exit 0
- `grep -q 'z.enum' packages/shared-validation/src/index.ts` → exit 0
</acceptance_criteria>
</task>

## Verification
- Run `pnpm install` from root → completes without error, creates `node_modules` and `pnpm-lock.yaml`
- `pnpm -r run type-check` succeeds across both packages
- `pnpm -r run lint` returns no errors

## must_haves
- Phase success criterion 1: `pnpm install` from root works (root package.json + workspace config in place)
- Foundation for criteria 2-5 (apps and packages can be added in subsequent waves)
