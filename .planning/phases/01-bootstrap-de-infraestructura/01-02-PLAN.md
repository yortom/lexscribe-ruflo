---
id: 01-02
phase: 1
wave: 1
depends_on: []
files_modified:
  - apps/frontend/package.json
  - apps/frontend/tsconfig.json
  - apps/frontend/next.config.mjs
  - apps/frontend/.eslintrc.cjs
  - apps/frontend/postcss.config.mjs
  - apps/frontend/tailwind.config.ts
  - apps/frontend/vitest.config.ts
  - apps/frontend/vitest.setup.ts
  - apps/frontend/app/layout.tsx
  - apps/frontend/app/page.tsx
  - apps/frontend/app/globals.css
  - apps/frontend/app/__tests__/page.test.tsx
  - apps/frontend/.env.example
requirements_addressed: [INF-01, INF-03]
autonomous: true
---

# Plan 01-02: Frontend skeleton — Next.js 14 App Router + Tailwind + Vitest

## Objective
Crear `apps/frontend` con Next.js 14+ (App Router) en modo `standalone` (preparado para Docker), Tailwind CSS, un layout raíz y una home placeholder, más Vitest + Testing Library con un test de humo. Sin lógica de dominio.

## Tasks

<task id="01-02-T1">
<title>Crear apps/frontend con Next.js 14 standalone + Tailwind</title>
<read_first>
- docs/ARQUITECTURA.md (sección 2 Stack, sección 4.1 Organización frontend, sección 10 Despliegue — frontend Next.js standalone build)
- tsconfig.base.json
- .planning/REQUIREMENTS.md (INF-01)
</read_first>
<action>
Crea la estructura de `apps/frontend`:

1. `apps/frontend/package.json`:
```json
{
  "name": "@lexscribe/frontend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@lexscribe/shared-types": "workspace:*",
    "@lexscribe/shared-validation": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.5.0",
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.5",
    "vitest": "^2.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "@testing-library/react": "^16.0.0",
    "@testing-library/jest-dom": "^6.4.0",
    "jsdom": "^24.0.0"
  }
}
```

2. `apps/frontend/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "noEmit": true,
    "jsx": "preserve",
    "incremental": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

3. `apps/frontend/next.config.mjs` — MUST set `output: 'standalone'` for Docker:
```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  reactStrictMode: true,
};
export default nextConfig;
```

4. `apps/frontend/.eslintrc.cjs`:
```js
module.exports = {
  extends: ['next/core-web-vitals'],
};
```

5. `apps/frontend/postcss.config.mjs`:
```js
export default {
  plugins: { tailwindcss: {}, autoprefixer: {} },
};
```

6. `apps/frontend/tailwind.config.ts`:
```ts
import type { Config } from 'tailwindcss';
const config: Config = {
  content: ['./app/**/*.{ts,tsx}', './components/**/*.{ts,tsx}'],
  theme: { extend: {} },
  plugins: [],
};
export default config;
```

7. `apps/frontend/app/globals.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

8. `apps/frontend/app/layout.tsx`:
```tsx
import './globals.css';
import type { ReactNode } from 'react';

export const metadata = {
  title: 'Lexscribe',
  description: 'Lexscribe — gestión de despacho legal',
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}
```

9. `apps/frontend/app/page.tsx`:
```tsx
export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <h1 className="text-3xl font-bold">Lexscribe</h1>
    </main>
  );
}
```

10. `apps/frontend/.env.example`:
```
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
```
</action>
<acceptance_criteria>
- `test -f apps/frontend/package.json && grep -q '"@lexscribe/frontend"' apps/frontend/package.json` → exit 0
- `grep -q '"next": "\^14' apps/frontend/package.json` → exit 0
- `grep -q '"workspace:\*"' apps/frontend/package.json` → exit 0 (shared-types/shared-validation linked via workspace protocol)
- `grep -q "output: 'standalone'" apps/frontend/next.config.mjs` → exit 0
- `test -f apps/frontend/app/layout.tsx && grep -q 'lang="es"' apps/frontend/app/layout.tsx` → exit 0
- `test -f apps/frontend/app/page.tsx && grep -q 'Lexscribe' apps/frontend/app/page.tsx` → exit 0
- `test -f apps/frontend/tailwind.config.ts && grep -q "'./app/\*\*/\*.{ts,tsx}'" apps/frontend/tailwind.config.ts` → exit 0
- `test -f apps/frontend/app/globals.css && grep -q '@tailwind base' apps/frontend/app/globals.css` → exit 0
</acceptance_criteria>
</task>

<task id="01-02-T2">
<title>Configurar Vitest + Testing Library con un test de humo</title>
<read_first>
- docs/ARQUITECTURA.md (sección 2: "Tests frontend: Vitest + Testing Library", sección 13 Tests)
- apps/frontend/package.json (just created in T1)
- apps/frontend/app/page.tsx (test will target this)
</read_first>
<action>
Configura Vitest + Testing Library en `apps/frontend`:

1. `apps/frontend/vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'node:path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./vitest.setup.ts'],
    include: ['app/**/*.test.{ts,tsx}', 'components/**/*.test.{ts,tsx}'],
  },
  resolve: {
    alias: { '@': path.resolve(__dirname, '.') },
  },
});
```

2. `apps/frontend/vitest.setup.ts`:
```ts
import '@testing-library/jest-dom/vitest';
```

3. `apps/frontend/app/__tests__/page.test.tsx` — smoke test that renders the home page:
```tsx
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import HomePage from '../page';

describe('HomePage', () => {
  it('renders the Lexscribe heading', () => {
    render(<HomePage />);
    expect(screen.getByRole('heading', { level: 1 })).toHaveTextContent('Lexscribe');
  });
});
```
</action>
<acceptance_criteria>
- `test -f apps/frontend/vitest.config.ts && grep -q "environment: 'jsdom'" apps/frontend/vitest.config.ts` → exit 0
- `grep -q "setupFiles: \['./vitest.setup.ts'\]" apps/frontend/vitest.config.ts` → exit 0
- `test -f apps/frontend/vitest.setup.ts && grep -q "@testing-library/jest-dom/vitest" apps/frontend/vitest.setup.ts` → exit 0
- `test -f apps/frontend/app/__tests__/page.test.tsx && grep -q "describe('HomePage'" apps/frontend/app/__tests__/page.test.tsx` → exit 0
- After `pnpm install` in root, `pnpm --filter @lexscribe/frontend test` exits 0 (test passes)
</acceptance_criteria>
</task>

## Verification
- `pnpm --filter @lexscribe/frontend type-check` → exit 0
- `pnpm --filter @lexscribe/frontend lint` → exit 0
- `pnpm --filter @lexscribe/frontend test` → exit 0 (smoke test passes)
- `pnpm --filter @lexscribe/frontend build` produces `.next/standalone` directory

## must_haves
- Phase success criterion 1: `pnpm dev` arranca el frontend (script `dev` defined)
- Phase success criterion 2: standalone build ready for Docker (`output: 'standalone'`)
- Phase success criterion 4: lint + type-check + test + build commands all wired (used by pr.yml in 01-05)
