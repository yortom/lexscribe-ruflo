---
id: 01-03
phase: 1
wave: 1
depends_on: []
files_modified:
  - apps/backend/package.json
  - apps/backend/tsconfig.json
  - apps/backend/tsconfig.build.json
  - apps/backend/nest-cli.json
  - apps/backend/.eslintrc.cjs
  - apps/backend/jest.config.ts
  - apps/backend/src/main.ts
  - apps/backend/src/app.module.ts
  - apps/backend/src/common/logger/logger.module.ts
  - apps/backend/src/modules/health/health.module.ts
  - apps/backend/src/modules/health/health.controller.ts
  - apps/backend/test/health.e2e-spec.ts
  - apps/backend/.env.example
requirements_addressed: [INF-01, INF-02, INF-03]
autonomous: true
---

# Plan 01-03: Backend skeleton — NestJS + Pino + Terminus health endpoints + Jest

## Objective
Crear `apps/backend` con NestJS, prefijo global `/api/v1`, logger Pino estructurado, y los endpoints `GET /api/v1/health` y `GET /api/v1/health/ready` usando `@nestjs/terminus`. Configurar Jest + Supertest con un test e2e que valide ambos endpoints.

## Tasks

<task id="01-03-T1">
<title>Inicializar apps/backend NestJS con prefijo /api/v1 y Pino logger</title>
<read_first>
- docs/ARQUITECTURA.md (sección 5 Backend NestJS, sección 6.1 Convenciones — Base URL `/api/v1/...`, sección 12 Observabilidad — Pino + Pino-pretty dev)
- tsconfig.base.json
- .planning/REQUIREMENTS.md (INF-01, INF-02)
</read_first>
<action>
Crea la estructura inicial de `apps/backend`:

1. `apps/backend/package.json`:
```json
{
  "name": "@lexscribe/backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "start": "node dist/main.js",
    "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\"",
    "type-check": "tsc --noEmit -p tsconfig.json",
    "test": "jest",
    "test:e2e": "jest --config jest.e2e.config.ts"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.0",
    "@nestjs/core": "^10.4.0",
    "@nestjs/platform-express": "^10.4.0",
    "@nestjs/terminus": "^10.2.3",
    "@nestjs/config": "^3.2.0",
    "nestjs-pino": "^4.1.0",
    "pino": "^9.3.0",
    "pino-http": "^10.2.0",
    "pino-pretty": "^11.2.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "@lexscribe/shared-types": "workspace:*",
    "@lexscribe/shared-validation": "workspace:*"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.0",
    "@nestjs/schematics": "^10.1.0",
    "@nestjs/testing": "^10.4.0",
    "@types/express": "^4.17.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "@types/supertest": "^6.0.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.0",
    "ts-loader": "^9.5.0",
    "ts-node": "^10.9.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.5.0",
    "eslint": "^8.57.0"
  }
}
```

2. `apps/backend/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "baseUrl": "./",
    "incremental": true,
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "module": "commonjs",
    "moduleResolution": "node"
  },
  "include": ["src/**/*", "test/**/*"]
}
```

3. `apps/backend/tsconfig.build.json`:
```json
{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts", "**/*.e2e-spec.ts"]
}
```

4. `apps/backend/nest-cli.json`:
```json
{
  "$schema": "https://json.schemastore.org/nest-cli",
  "collection": "@nestjs/schematics",
  "sourceRoot": "src"
}
```

5. `apps/backend/.eslintrc.cjs`:
```js
module.exports = {
  parser: '@typescript-eslint/parser',
  parserOptions: { project: 'tsconfig.json', sourceType: 'module' },
  plugins: ['@typescript-eslint'],
  extends: ['eslint:recommended', 'plugin:@typescript-eslint/recommended'],
  env: { node: true, jest: true },
  ignorePatterns: ['dist', 'node_modules'],
};
```

6. `apps/backend/src/common/logger/logger.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { LoggerModule as PinoLoggerModule } from 'nestjs-pino';

@Module({
  imports: [
    PinoLoggerModule.forRoot({
      pinoHttp: {
        transport:
          process.env.NODE_ENV !== 'production'
            ? { target: 'pino-pretty', options: { singleLine: true } }
            : undefined,
        level: process.env.LOG_LEVEL ?? 'info',
      },
    }),
  ],
})
export class LoggerModule {}
```

7. `apps/backend/src/app.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { LoggerModule } from './common/logger/logger.module';
import { HealthModule } from './modules/health/health.module';

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    LoggerModule,
    HealthModule,
  ],
})
export class AppModule {}
```

8. `apps/backend/src/main.ts` — MUST set global prefix `api/v1` and listen on PORT (default 3001):
```ts
import { NestFactory } from '@nestjs/core';
import { Logger } from 'nestjs-pino';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule, { bufferLogs: true });
  app.useLogger(app.get(Logger));
  app.setGlobalPrefix('api/v1');
  const port = Number(process.env.PORT ?? 3001);
  await app.listen(port, '0.0.0.0');
}
bootstrap();
```

9. `apps/backend/.env.example`:
```
NODE_ENV=development
PORT=3001
LOG_LEVEL=info
MONGO_URI=mongodb://localhost:27017/lexscribe
```
</action>
<acceptance_criteria>
- `test -f apps/backend/package.json && grep -q '"@lexscribe/backend"' apps/backend/package.json` → exit 0
- `grep -q '"@nestjs/terminus"' apps/backend/package.json` → exit 0
- `grep -q '"nestjs-pino"' apps/backend/package.json` → exit 0
- `grep -q '"workspace:\*"' apps/backend/package.json` → exit 0
- `test -f apps/backend/src/main.ts && grep -q "setGlobalPrefix('api/v1')" apps/backend/src/main.ts` → exit 0
- `grep -q "process.env.PORT ?? 3001" apps/backend/src/main.ts` → exit 0
- `test -f apps/backend/src/common/logger/logger.module.ts && grep -q "pino-pretty" apps/backend/src/common/logger/logger.module.ts` → exit 0
- `test -f apps/backend/nest-cli.json && grep -q '"sourceRoot": "src"' apps/backend/nest-cli.json` → exit 0
</acceptance_criteria>
</task>

<task id="01-03-T2">
<title>Implementar HealthModule con Terminus — endpoints /health y /health/ready</title>
<read_first>
- docs/ARQUITECTURA.md (sección 12 Observabilidad — Healthchecks: NestJS Terminus, /health y /health/ready)
- apps/backend/src/app.module.ts (just created in T1)
- .planning/REQUIREMENTS.md (INF-02)
</read_first>
<action>
Crea el módulo health usando `@nestjs/terminus`. Dos endpoints: `/health` (liveness — siempre 200 si la app responde) y `/health/ready` (readiness — incluye placeholder para checks de dependencias futuras como Mongo y MinIO).

1. `apps/backend/src/modules/health/health.controller.ts`:
```ts
import { Controller, Get } from '@nestjs/common';
import {
  HealthCheck,
  HealthCheckService,
  HealthCheckResult,
  HealthIndicatorResult,
} from '@nestjs/terminus';

@Controller('health')
export class HealthController {
  constructor(private readonly health: HealthCheckService) {}

  @Get()
  @HealthCheck()
  liveness(): Promise<HealthCheckResult> {
    return this.health.check([
      async (): Promise<HealthIndicatorResult> => ({
        app: { status: 'up' },
      }),
    ]);
  }

  @Get('ready')
  @HealthCheck()
  readiness(): Promise<HealthCheckResult> {
    // Placeholder: dependency checks (Mongo, MinIO) added in later phases
    return this.health.check([
      async (): Promise<HealthIndicatorResult> => ({
        app: { status: 'up' },
      }),
    ]);
  }
}
```

2. `apps/backend/src/modules/health/health.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { TerminusModule } from '@nestjs/terminus';
import { HealthController } from './health.controller';

@Module({
  imports: [TerminusModule],
  controllers: [HealthController],
})
export class HealthModule {}
```
</action>
<acceptance_criteria>
- `test -f apps/backend/src/modules/health/health.controller.ts && grep -q "@Controller('health')" apps/backend/src/modules/health/health.controller.ts` → exit 0
- `grep -q "@Get()" apps/backend/src/modules/health/health.controller.ts && grep -q "@Get('ready')" apps/backend/src/modules/health/health.controller.ts` → exit 0
- `grep -q "HealthCheckService" apps/backend/src/modules/health/health.controller.ts` → exit 0
- `test -f apps/backend/src/modules/health/health.module.ts && grep -q "TerminusModule" apps/backend/src/modules/health/health.module.ts` → exit 0
- `grep -q "HealthModule" apps/backend/src/app.module.ts` → exit 0
</acceptance_criteria>
</task>

<task id="01-03-T3">
<title>Configurar Jest + Supertest con e2e test para health endpoints</title>
<read_first>
- docs/ARQUITECTURA.md (sección 13 Tests — Jest + Supertest backend)
- apps/backend/src/app.module.ts (just created)
- apps/backend/src/modules/health/health.controller.ts (just created)
</read_first>
<action>
Configura Jest unitario + e2e con un test que arranca la app y verifica los dos endpoints de salud.

1. `apps/backend/jest.config.ts` (unit + e2e via include patterns):
```ts
import type { Config } from 'jest';

const config: Config = {
  moduleFileExtensions: ['js', 'json', 'ts'],
  rootDir: '.',
  testRegex: '.*\\.(spec|e2e-spec)\\.ts$',
  transform: { '^.+\\.(t|j)s$': 'ts-jest' },
  collectCoverageFrom: ['src/**/*.(t|j)s'],
  coverageDirectory: './coverage',
  testEnvironment: 'node',
  moduleNameMapper: {
    '^@lexscribe/(.*)$': '<rootDir>/../../packages/$1/src',
  },
};
export default config;
```

2. `apps/backend/test/health.e2e-spec.ts`:
```ts
import { Test, TestingModule } from '@nestjs/testing';
import { INestApplication } from '@nestjs/common';
import * as request from 'supertest';
import { AppModule } from '../src/app.module';

describe('Health endpoints (e2e)', () => {
  let app: INestApplication;

  beforeAll(async () => {
    const moduleRef: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();

    app = moduleRef.createNestApplication();
    app.setGlobalPrefix('api/v1');
    await app.init();
  });

  afterAll(async () => {
    await app.close();
  });

  it('GET /api/v1/health returns 200 and status ok', async () => {
    const res = await request(app.getHttpServer()).get('/api/v1/health');
    expect(res.status).toBe(200);
    expect(res.body.status).toBe('ok');
  });

  it('GET /api/v1/health/ready returns 200 and status ok', async () => {
    const res = await request(app.getHttpServer()).get('/api/v1/health/ready');
    expect(res.status).toBe(200);
    expect(res.body.status).toBe('ok');
  });
});
```
</action>
<acceptance_criteria>
- `test -f apps/backend/jest.config.ts && grep -q "spec|e2e-spec" apps/backend/jest.config.ts` → exit 0
- `test -f apps/backend/test/health.e2e-spec.ts && grep -q "/api/v1/health" apps/backend/test/health.e2e-spec.ts` → exit 0
- `grep -q "/api/v1/health/ready" apps/backend/test/health.e2e-spec.ts` → exit 0
- `grep -q "expect(res.status).toBe(200)" apps/backend/test/health.e2e-spec.ts` → exit 0
- After `pnpm install`, running `pnpm --filter @lexscribe/backend test` exits 0 (both health tests pass)
</acceptance_criteria>
</task>

## Verification
- `pnpm --filter @lexscribe/backend type-check` → exit 0
- `pnpm --filter @lexscribe/backend lint` → exit 0
- `pnpm --filter @lexscribe/backend test` → exit 0 (e2e tests pass)
- `pnpm --filter @lexscribe/backend build` produces `dist/main.js`
- Manual: `pnpm --filter @lexscribe/backend dev`, then `curl http://localhost:3001/api/v1/health` → 200; `curl http://localhost:3001/api/v1/health/ready` → 200

## must_haves
- Phase success criterion 1: `pnpm dev` arranca el backend
- Phase success criterion 3: `GET /api/v1/health` y `GET /api/v1/health/ready` responden 200 (verified by e2e test)
- Phase success criterion 4: lint + type-check + test + build commands all wired (used by pr.yml in 01-05)
